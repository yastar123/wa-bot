## Packages
socket.io-client | Real-time communication with backend
qrcode.react | Rendering QR code for login
date-fns | Formatting timestamps
framer-motion | Animations and transitions
clsx | Class name utility (often paired with tailwind-merge)
tailwind-merge | Class name merging (often paired with clsx)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
WebSocket connects to root '/' path
